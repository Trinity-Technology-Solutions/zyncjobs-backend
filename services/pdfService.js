const pdfService = {
  generateResumePDF: async (resumeData) => {
    throw new Error('PDF generation not implemented. Install puppeteer or pdfkit to enable this feature.');
  }
};

export default pdfService;
