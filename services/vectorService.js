const vectorService = {
  findSimilarJobs: async (text) => [],
  findSimilarCandidates: async (text) => [],
  upsertJobEmbedding: async (jobId, jobData) => {},
  upsertResumeEmbedding: async (userId, resumeData) => {}
};

export default vectorService;
